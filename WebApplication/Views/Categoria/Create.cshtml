@model WebApplication.Models.Request.CriarCategoriaRequest

@{
    ViewData["Title"] = "Cadastrar Categoria";
}

<div class="container mt-5">
    <div class="row">
        <h2>Cadastrar Categoria</h2>
        <hr />
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="NomeCategoria" class="control-label"></label>
                    <input asp-for="NomeCategoria" class="form-control" />
                    <span asp-validation-for="NomeCategoria" class="text-danger"></span>
                </div>
                <hr />
                <div class="btn-group" role="group">
                    <button class="btn btn-primary" type="submit" id="salvarCategoria">Salvar</button>
                    <button class="btn btn-dark">Voltar</button>
                </div>
            </form>
            <div class="toast position-fixed bottom-0 end-0 p-3" style="z-index: 11">
                <div id="salvarCategoria" class="toast hide" role="alert" aria-live="assertive" aria-atomic="true">
                    <div class="toast-header">
                        <strong class="me-auto">Cadastro de categoria</strong>
                        <small>Agora</small>
                        <button type="button" class="btn-close" data-bs-dismiss="toast"></button>
                    </div>
                    <div class="toast-body">
                        <p>O registro está sendo gravado na base de dados.</p>
                        <p>Por favor aguarde...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    document.getElementById("salvarCategoria").onclick = function () {
        var toastElList = [].slice.call(document.querySelectorAll('.toast'))
        var toastList = toastElList.map(function (toastEl) {
            return new bootstrap.Toast(toastEl)
        })
        toastList.forEach(toast => toast.show())
    }
</script>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
